worker_processes 1;
events { worker_connections 1024; }
http {
    include mime.types;
    sendfile on;

    server {
        listen <%= ENV["PORT"] %>;
        root /app/dist;
        index index.html;

        location / {
            try_files $uri /index.html;
        }
    }
}